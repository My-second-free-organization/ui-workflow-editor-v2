name: Perf Test Workflow 1

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'perf-test/**'

jobs:
  synthetic-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Synthetic validation
        run: |
          echo "Repository: ui-workflow-editor-v2"
          echo "Run ID: perf-20260522-064709"
          echo "This workflow is generated for API performance testing."
